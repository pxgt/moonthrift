# MoonThrift 项目申报书

## 基本信息

- 项目名称：MoonThrift——MoonBit 的 Apache Thrift IDL 与协议工具库
- 参赛者：彭翔
- 联系方式：18589205060
- GitHub 仓库：https://github.com/pxgt/moonthrift
- 项目方向：序列化协议 / IDL 工具链 / RPC 基础设施
- 是否为移植项目：否，原创实现，兼容行为参考 Apache Thrift 公开规范

## 项目简介

MoonThrift 为 MoonBit 提供一套可复用的 Thrift 基础能力。项目可以读取和检查
Thrift IDL，把 Schema 生成 MoonBit 数据类型，也可以在没有生成代码的情况下用
动态值读写 Binary Protocol 和 Compact Protocol。它适合用在 RPC 框架、协议调试、
Schema 管理、接口升级检查和数据迁移工具中。

## 核心功能

- 解析 include、namespace、const、typedef、enum、struct、union、exception、
  service、throws、容器类型和 annotations，并保留源码位置；
- 检查重复定义、字段 ID 冲突、未解析类型、union 和 oneway 等语义规则；
- 提供带资源上限的 Binary/Compact 动态编解码和完整 RPC 消息封装；
- 从已检查的 Schema 生成可编译的 MoonBit typedef、enum、record、union 及
  service 参数/结果模型；
- 比较两个 IDL 版本，按 compatible、warning、breaking 标出字段、枚举和服务
  接口变化；
- 提供 check、inspect、generate、diff 命令，可直接运行仓库内示例；
- 在 wasm、wasm-gc、JavaScript、native 四个后端执行测试和 CI。

## 原创与参考说明

本项目不是对现有代码库的逐行移植，解析器、数据模型、编解码器、生成器和兼容性
检查均使用 MoonBit 重新设计和实现。协议常量及线格式参考 Apache Thrift 官方文档
和仓库（https://thrift.apache.org/ 、https://github.com/apache/thrift），上游许可证为
Apache License 2.0；本项目同样采用 Apache-2.0。0.1.0 暂不实现网络 transport、
服务端调度和 TLS，这些能力后续可在当前可移植核心之上独立扩展。
